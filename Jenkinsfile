pipeline {
        agent any
        stages {
               stage ( "Run code"){
                       steps {
                             echo "The branch : mybranch"
                       }  
               }

        }
}
